{
  "name": "tetrad-bot-client",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone LLM player for Tetrad — joins a room over WebSocket and plays via OpenRouter. Intentionally NOT part of the main workspace; imports nothing from the main codebase.",
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "spawner": "tsx src/spawner.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
